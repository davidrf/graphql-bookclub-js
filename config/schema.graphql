type Query {
  myArrayField: [String!]!
  myBooleanField: Boolean!
  myIntegerField: Int!
  myTextField: String!
  repository(id: ID!): Repository
  testField: String!
  user(id: ID!): User
  users: [User!]!
}

type Repository {
  id: ID!
  name: String!
}

type User {
  bio: String
  firstName: String!
  fullName: String!
  id: ID!
  lastName: String!
  pictureUrl: String
  username: String!
}
